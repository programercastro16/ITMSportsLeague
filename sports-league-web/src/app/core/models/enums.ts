export enum TournamentStatus {
  Pending = 0,
  InProgress = 1,
  Finished = 2,
}

export enum SponsorCategory {
  Main = 0,
  Gold = 1,
  Silver = 2,
  Bronze = 3,
}

export enum PlayerPosition {
  Goalkeeper = 0,
  Defender = 1,
  Midfielder = 2,
  Forward = 3,
}
